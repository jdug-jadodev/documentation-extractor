schema_version: 1
name: run-manifest
required:
  - run_id
  - repository
  - branch
  - scope
  - status
  - analyzed_at
properties:
  scope: [inventory, standard, full, targeted]
  status: [pending, analyzing, review, published, failed]
  validation: [schema, references, coverage, human_review]
